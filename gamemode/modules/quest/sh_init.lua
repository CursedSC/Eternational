/*local function a(a)
    return !isentity(a) and !isfunction(a)
end

function questData(tbl)
    local tblReturn = {}
    for k, v in pairs(tbl) do
        if a(v) then tblReturn[k] = v end
    end

    return tblReturn
end*/
